<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:GlutenFree.ViewModels" 
             x:DataType="viewmodels:LoginViewModel"
             x:Class="GlutenFree.Views.LoginPage"
             Shell.NavBarIsVisible="True"
             Shell.TabBarIsVisible="False"
             Title="{Binding Title}"
             BackgroundColor="{AppThemeBinding 
                        Dark={StaticResource WindowBackgroundColorDark}, 
                        Light={StaticResource WindowBackgroundColor}}" >

    <StackLayout Padding="40" VerticalOptions="Center"
                     HorizontalOptions="FillAndExpand" >
        <Label Text="Username:" Style="{StaticResource BaseLabel}"/>
        <Entry x:Name="emailEntry" Placeholder="Username" 
            Style="{StaticResource Entry}" Text="{Binding EmailEntry}"/>
        <Label Text="Password:" Style="{StaticResource BaseLabel}"/>
        <Entry x:Name="usernameEntry" IsPassword="True" 
               Placeholder="Password" Style="{StaticResource Entry}" 
               Text="{Binding PasswordEntry}" />
        <Button Text="Login" 
                Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:LoginViewModel}}, Path=LoginButtonTapped}" />
        <Label Text="Register" HorizontalOptions="Center" Style="{StaticResource BaseLabel}" >
            <Label.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodels:LoginViewModel}}, Path=RegistrationButtonTapped}" />
            </Label.GestureRecognizers>
        </Label>
    </StackLayout>

</ContentPage>